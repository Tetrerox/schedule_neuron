// MyUser is a makeshift class of AuthServiceUser
// so that we can have better control over the 
// features (variables) that we need and want from
// AuthServiceUser which includes 10+ of them.
class MyUser {
  final String uid;

  MyUser(this.uid);
}